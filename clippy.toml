# `..` keeps clippy's defaults and adds the product names these crates' docs
# name in prose. Without these, `doc_markdown` wants backticks around every
# mention of a database or a platform.
doc-valid-idents = ["SQLite", "PostgreSQL", "MySQL", "Cloudflare", "JavaScript", ".."]
